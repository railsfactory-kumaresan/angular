require "spec_helper"

describe QuestionStyle do
  # it { should belong_to(:question) }
  # it { should validate_presence_of(:background) }
  # it { should validate_presence_of(:page_header) }
  # it { should validate_presence_of(:submit_button) }
  # it { should validate_presence_of(:button_text) }
  # it { should validate_presence_of(:question_id) }
  # it { should validate_presence_of(:answers) }
  # it { should validate_presence_of(:font_style) }
  # it { should validate_presence_of(:question_text) }
  # it { should validate_numericality_of(:question_id) }
end